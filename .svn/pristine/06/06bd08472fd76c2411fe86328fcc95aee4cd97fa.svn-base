package TestsPackage;

import static org.junit.Assert.*;

import java.util.ArrayList;

import models.PlotableTrajectory;
import models.Position;
import models.Trajectory;

import org.junit.Test;

public class PlotableTrajectoryTest {

	@Test
	public void test() {
		Position p1 = new Position(1,2,3);
		Position p2 = new Position(3,4,5);
		PlotableTrajectory pTraj = new PlotableTrajectory(p1, 2.0, 3.0);
		ArrayList<Position> positions = new ArrayList();
		positions.add(p1);
		positions.add(p2);
		Trajectory t1 = new Trajectory(positions);
		
	}

}
